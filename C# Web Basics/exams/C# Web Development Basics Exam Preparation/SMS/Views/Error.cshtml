<div class="alert alert-danger alert-dismissable">
    <a href="javascript:window.history.back()" class="close" aria-label="close">×</a>
    <strong>Error!</strong>
    @foreach (var error in Model)
    {
        @error
    }
</div>